# 자료 및 변경 관리

## 공개 저장소와 로컬 시험 자료 분리

저장소는 공개 상태입니다. `.gitignore`는 초기 allowlist 방식입니다. `git add .`, `git add -f`, 원본 프로젝트 통째 업로드는 사용하지 않습니다. 명시적인 파일 목록을 검토해 등록합니다.

등록 대상: 자체 작성 소스/스크립트/제약/문서, 출처와 라이선스가 확인된 upstream 고정 버전 또는 변경분, 비식별화한 합성 시험 fixture와 작은 결과 요약.

등록 금지: 상용 ROM/boot ROM, 사용자 SAV/SRM, 메모리 dump/영상, 라이선스/키/토큰, 설치 도구/캐시, 사적인 절대 경로가 담긴 무검토 로그. 바이너리 배포는 소스·라이선스·후보 검증이 완료된 뒤 별도 판단합니다.

third-party 코드를 옮길 때는 원래 라이선스·저작권 표시를 보존하고 upstream URL/commit과 변경 내역을 기록합니다. 프로젝트 전체에 임의의 라이선스를 붙여 upstream 조건을 덮지 않습니다.

## 변경 단위

작업 브랜치는 `codex/` 접두사를 사용합니다. 한 변경은 한 가설과 연관된 소스·시험·결과 요약으로 구성합니다. main/default branch를 force-push하지 않습니다. 후보 ID, 소스/SDC hash, 도구 버전, 시험 범위와 미결 사항을 남깁니다.

단위 시험, 오류 주입, 동일 후보 통합, 제약 완전성/CDC 검토, 물리 fit, 실기를 구분합니다. 모델 pass나 양수 slack만으로 실기/제품 완료를 선언하지 않습니다. 무열화 실패를 숨기기 위해 비교 기준·클록·색·프레임 정책을 몰래 바꾸지 않습니다.

## 최초 후속 과제

1. 원본 시험 작업 공간의 독립 사본과 해시 manifest 준비. 공개 Git 저장소와 혼합하지 않기.
2. 소스/의존성/라이선스 inventory 작성 후 필요한 RTL/펌웨어/빌드 스크립트를 allowlist에 추가.
3. 인터페이스/클록/리셋/제약 미결 항목을 추적할 verification matrix 작성.
4. ROM 없이 가능한 lint/단위 검사를 우선 자동화. Questa/Quartus 및 개인 ROM 회귀는 로컬 실행으로 분리. 클라우드 CI에 라이선스 파일을 넣지 않기.
